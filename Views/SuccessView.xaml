<UserControl x:Class="ProjectIRON.SuccessView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ProjectIRON"
             mc:Ignorable="d"
             d:DesignHeight="700"
             d:DesignWidth="1000">

    <Grid Background="#17191C">

        <StackPanel
            Width="450"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">

            <TextBlock
                Text="Workout Saved"
                Foreground="White"
                FontSize="40"
                FontWeight="Bold"
                HorizontalAlignment="Center"
                Margin="0,0,0,15"/>

            <TextBlock
                Text="✓"
                Foreground="LimeGreen"
                FontSize="62"
                FontWeight="Bold"
                HorizontalAlignment="Center"
                Margin="0,0,0,30"/>

            <StackPanel
                Orientation="Horizontal"
                HorizontalAlignment="Center">

                <Button
                    Content="Log Another Workout"
                    Width="200"
                    Height="60"
                    FontSize="18"
                    Margin="0,0,20,0"
                    Click="LogAnotherButton_Click"/>

                <Button
                    Content="Back to Home"
                    Width="200"
                    Height="60"
                    FontSize="18"
                    Click="HomeButton_Click"/>

            </StackPanel>

        </StackPanel>

    </Grid>
</UserControl>