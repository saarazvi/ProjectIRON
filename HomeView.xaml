<UserControl x:Class="ProjectIRON.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ProjectIRON"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <Grid Background="#17191C">

        <StackPanel
            Width="280"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">

            <TextBlock
                Text="Welcome!"
                Foreground="White"
                FontSize="26"
                FontWeight="Bold"
                HorizontalAlignment="Center"
                Margin="0,0,0,12"/>

            <TextBlock
                Text="What would you like to do today?"
                Foreground="#B8B8B8"
                FontSize="15"
                HorizontalAlignment="Center"
                Margin="0,0,0,30"/>

            <Button
                Content="Log Weight"
                Height="48"
                Margin="0,0,0,14"
                FontSize="15"/>

            <Button
                Content="Log Workout"
                Height="48"
                Margin="0,0,0,14"
                FontSize="15"
                Click="LogWorkoutButton_Click"/>

            <Button
                Content="View Logs"
                Height="48"
                Margin="0,0,0,14"
                FontSize="15"/>

            <Button
                Content="Exit"
                Height="48"
                FontSize="15"/>

        </StackPanel>

    </Grid>

</UserControl>